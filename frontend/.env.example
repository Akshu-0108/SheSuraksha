# Leave unset for local development (defaults to http://localhost:5000/api).
# Set this to the deployed backend URL in production, without a trailing slash.
VITE_API_BASE_URL=https://your-api.example.com/api
